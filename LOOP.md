# Merge Mage Loop Log

| # | date | maker | what ran | what broke | what got fixed |
| 1 | 2026-07-02 | codex(gpt-5.5) scaffolded Phaser 4.2+Vite 8.1+Hono skeleton | deployed to Vercel, smoke-tested / (game) and /api/health | /api/health hung then 504: Vercel ignored the edge runtime config, edge-style Hono adapter never answered on the Node runtime | switched to @hono/node-server v1 /vercel adapter (v2 dropped the subpath), health now returns {"ok":true} — checker: vercel logs + live smoke (TestSprite suite seeding starts with first real gameplay) |
| 2 | 2026-07-02 | 3 parallel codex agents built api (Neon save/leaderboard/offline, 36 schema tests), battle scene (pooled projectiles/damage numbers/boss), and React UI+game driver | integrated build+29 unit tests green; manual preview play-through: summon->merge->LV2 verified via DOM state mirror | tap-to-equip broken: tapping an equip slot with a book selected triggers the nested UP(grade) button instead — no equip + accidental 50g spend; dev-mode save toast noisy (404, no /api in vite dev) | queued for wave 3 fix; deploying as-is so the TestSprite suite can be seeded against the live URL first |
| 3 | 2026-07-02 | orchestrator deployed wave-2 fullstack to Vercel prod | live smoke: /, /api/health, /api/leaderboard, autosave | every API route 500 FUNCTION_INVOCATION_FAILED — (a) Vercel built every api/*.ts (helpers + even the test file) as separate functions, (b) after regrouping to api/_lib, Node ESM rejected extensionless relative imports at runtime | moved helpers to api/_lib (single index.func), codemodded .js extensions onto 18 files' relative imports, verified the built .func loads locally before redeploy — prod now 200s, autosave clean |
| 4 | 2026-07-02 | TestSprite round 1: 7 FE plans + 4 BE scripts vs live URL | FE 3 passed (merge, skills, rebirth-lock) / 3 failed / 1 blocked; BE 4 failed | checker caught: (a) summon button showed 24 gold but charged 24.4 — float cost, display != charge; (b) equipping spent gold via the nested UP button; (c) RANKS error; (d) every BE POST timed out 30s+ — Vercel node helpers consume the body stream before Hono reads it | (a) getSummonCost now ceil'd at the source + test; (b) equip/upgrade hitbox+state separation in wave 3; (d) replaced adapter with a hand-rolled handler that rebuilds a web Request from the helper-parsed body — all endpoints answer in ~0.3s |
| 5 | 2026-07-03 | wave 3 skin integration play-test (director, local preview) | fresh boot after skin: scene stuck in "booting", canvas black | two boot bugs: React StrictMode's double effect destroyed the Phaser game mid-asset-load; separately Phaser 4.2's loader stalls when the first 32-file parallel batch completes (0 inflight, 94 pending) | create-once singleton guard for the game; maxParallelDownloads=256 keeps all 126 assets in one batch (upstream candidate); loader now completes 126/126 and the skinned game renders |
| 6 | 2026-07-03 | TestSprite round 2+3 vs skinned deploy | round 2: summon+autobuy fixed->passed, merge "failed"; round 3: BE 4/4 all passed, FE 8/11 | checker caught: (a) level badge "2" reads as a stack count, agent (and humans) misread merged books; (b) leaderboard seam bugs from parallel agents — UI POST sent {nickname,stage} vs API {token,nickname}, and GET parser looked for entries[].stage vs API items[].bestStage; (c) test-to-test browser state leaked, auto-buy started broke | Lv-prefixed badges; both leaderboard contract sides aligned + save-before-submit; /?fresh=1 reset affordance + all FE plans start fresh |
| 7 | 2026-07-03 | TestSprite round 4 + targeted reruns vs current deploy | ALL GREEN: FE 7/7 + BE 4/4 (leaderboard finally green after the GET-parse fix; earlier "blocked" runs were concurrency with our own CI gate) | — | milestone: the full suite passes against the live app; CI gate (.github/workflows/testsprite.yml) now re-verifies every push — first Actions run green in 3m23s |
| 8 | 2026-07-03 | human playtest (the most honest checker) + two polish agents | owner played the deployed build cold | "there is nothing to do" — new players saw a dead screen: nothing equipped, no attack, no guidance; also a fully-broke old save was a hard softlock | genre-standard onboarding: innate arcane staff bolt (alive from second zero, softlock impossible), summons auto-equip into empty slots, contextual hint strip, empty-tab copy, faster first waves; then release polish: splash+loading bar, settings (sound/new-game/credits), SAVED chip, mobs 2x, coin arcs, boss enrage vignette + slow-mo kill, level-up ring |
| 9 | 2026-07-03 | TestSprite round 5 + serialized reruns vs polished deploy | ALL GREEN again: FE 7/7 + BE 4/4 on the release build | two systemic test-design catches this round: (a) plans still asserted the pre-auto-equip flow ("book appears in inventory") — features moved, tests must move; (b) any precise gold-delta assertion loses a race against idle combat income; also we rate-limited ourselves (60 triggers/min) by letting CI, manual suites, and docs-push triggers overlap | plans rewritten around user-visible outcomes (arithmetic lives in the 46 engine unit tests); CI now skips docs-only pushes, cancels overlapping runs, retries once after rate-limit backoff |
| 10 | 2026-07-03 | lazycodex release gate (executor->QA->xhigh gate reviewer, evidence under .omo/evidence) | 6 criteria vs the live deploy: cold start, core loop, persistence, server authority (adversarial), settings, judge surface | gate REJECTED: on the exact 390x844 mobile viewport the settings popover was swallowed by the HUD panel's clip-path — present in DOM, invisible, hit-test returned the canvas; every other criterion passed (summon cost deducted exactly, Lv9+Lv9->Lv10 floor rise, 8h offline cap, leaderboard ignores client stage) | popover re-parented out of the clipped panel + explicit pointer-events; hit-test now returns the button and sound/new-game work; verified with the gate's own methodology |
